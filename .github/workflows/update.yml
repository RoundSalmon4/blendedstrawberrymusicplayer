name: Update from Upstream

on:
  schedule:
    - cron: '0 * * * *'  # This runs the action every hour
  workflow_dispatch:  # Allows manual triggering of the workflow

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          repository: RoundSalmon4/test
          token: ${{ secrets.GITHUB_TOKEN }}
          path: ./repo

      - name: Change directory to repo
        run: cd ./repo

      - name: Add upstream remote
        run: |
          cd ./repo
          git remote add upstream https://github.com/strawberrymusicplayer/strawberry || echo "Upstream remote already exists."

      - name: Fetch upstream changes
        run: |
          cd ./repo
          git fetch upstream

      - name: Check out master branch
        run: |
          cd ./repo
          git checkout master

      - name: Check for changes
        id: check_changes
        run: |
          cd ./repo
          git merge upstream/master --strategy-option=theirs -- . ':!/.github/workflows' || echo "No changes to merge."

      - name: Push changes if any
        if: steps.check_changes.outcome == 'success'
        run: |
          cd ./repo
          git push origin master || echo "Push failed, no changes to push."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
